mixin swiper(images, description)
    .swiper.gallery__swiper
        .swiper-wrapper
            each image, index in images
                .swiper-slide
                    img(src= image, alt= description)

        .swiper-button-next
        .swiper-button-prev
        .swiper-pagination